<h2><%= @post.title %></h2>
<img src="<%= @post.image.url (:large) %>"></img>
<p><%= @post.content %></p>
<% if @post.user == current_user %>
<%= link_to "edit", edit_post_path(@post) %>
<%= link_to "remove", @post, method: :delete, data: {confirm: "Are you sure?"} %>
<% end %>