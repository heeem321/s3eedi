<% if flash %>
    <% flash.each do |type, message| %>
        <% if %w(alert notice info error).include? type %>
            <div class="alert alert-dismissible fade in <%= alert_class_for_flash_type(type) %>">
            <button class="close" data-dismiss="alert" type="button">
                <span aria-hidden="true">×</span>
                <span class="sr-only">Close</span>
            </button>
            <%= message.html_safe %>
            </div>
        <% end %>
    <% end %>
<% end %>
